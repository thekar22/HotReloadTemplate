```sh
yarn install // fetch dependencies

* npm run watch:dev, navigate to http://localhost:8080
	* `set up hot loading for html, sass, and es2015 javascript (index.html is located within /build folder, everything else will be in /app folder)`

* npm run watch:scraper, run node import_out/scrapper.js
	* `work with es2015 javascript in import/scraper.js; javascript will be transpiled to compliant javascript in import_out/scraper.js when npm script is running`

```
